---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

const selectedTag = Astro.url.searchParams.get('tag');

const allPosts = await getCollection('leetcode');
const posts = allPosts
    .filter(post => !selectedTag || post.data.tags?.includes(selectedTag))
    .sort((a, b) => (b.data.date?.getTime() ?? 0) - (a.data.date?.getTime() ?? 0));

const formatDate = (date: Date | undefined) => {
    if (!date) return '';
    return date.toLocaleDateString('en-US', {
        year: 'numeric',
        month: 'long',
        day: 'numeric',
    });
};
---
<Layout title="LeetCode Problems">
    <div class="blog-header">
        <h1>LeetCode Problems</h1>
        <p>A monthly series exploring algorithmic challenges and solutions.</p>
        {selectedTag && (
            <div class="filter-info">
                EXEC_QUERY --tag <span class="active-tag">#{selectedTag}</span>
                <a href="/leetcode" class="clear-filter">[RESET]</a>
            </div>
        )}
    </div>
    
    <div class="posts-list">
        {posts.map(post => (
            <article class="post-item">
                <div class="post-meta">
                    <span class="post-date">ID: {post.id.replace(/\.md$/, '')}</span>
                    {post.data.tags && (
                        <div class="post-tags">
                            {post.data.tags.map(tag => (
                                <a href={`/leetcode?tag=${tag}`} class={`tag ${selectedTag === tag ? 'active' : ''}`}>
                                    #{tag}
                                </a>
                            ))}
                        </div>
                    )}
                </div>
                <a href={`/leetcode/${post.id.replace(/\.md$/, '')}`} class="post-link">
                    <h2>{post.data.title || post.id.replace(/\.md$/, '').replace(/-/g, ' ')}</h2>
                    <p class="read-more">CAT solution.txt →</p>
                </a>
            </article>
        ))}
    </div>
</Layout>

<style>
    .blog-header { margin-bottom: 4rem; margin-top: 2rem; }
    .blog-header p { color: var(--secondary-text); font-size: 1.25rem; margin-bottom: 1.5rem; }
    
    .filter-info {
        font-size: 0.9rem;
        color: var(--secondary-text);
        display: flex;
        align-items: center;
        gap: 1rem;
        background: rgba(0, 59, 0, 0.1);
        padding: 0.75rem 1.25rem;
        border: 1px solid var(--border-color);
        width: fit-content;
    }

    .active-tag { color: var(--accent-color); font-weight: 700; }
    .clear-filter { color: var(--accent-color); text-decoration: none; font-size: 0.8rem; font-weight: 700; }
    .clear-filter:hover { text-shadow: 0 0 8px var(--accent-color); }

    .posts-list { display: grid; gap: 1.5rem; }
    
    .post-item {
        padding: 2rem;
        background-color: rgba(0, 59, 0, 0.05);
        border: 1px solid var(--border-color);
        transition: all 0.3s ease;
    }
    
    .post-item:hover {
        border-color: var(--accent-color);
        background: rgba(0, 255, 65, 0.05);
        box-shadow: 0 0 15px rgba(0, 255, 65, 0.1);
    }
    
    .post-link { text-decoration: none; color: inherit; }
    
    .post-meta {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.5rem;
    }

    .post-date {
        font-size: 0.8rem;
        color: var(--secondary-text);
        font-weight: 600;
    }

    .post-tags {
        display: flex;
        gap: 0.75rem;
    }

    .tag {
        font-size: 0.75rem;
        color: var(--secondary-text);
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        text-decoration: none;
        padding: 0.2rem 0.5rem;
        border: 1px solid transparent;
        transition: all 0.2s;
    }

    .tag:hover, .tag.active {
        color: var(--accent-color);
        border-color: var(--accent-color);
    }

    .post-item h2 { margin: 0; font-size: 1.5rem; text-transform: uppercase; border: none; text-shadow: 0 0 8px rgba(0, 255, 65, 0.3); }
    .read-more { margin-top: 1.5rem; color: var(--accent-color); font-weight: 700; font-size: 0.9rem; }
</style>
